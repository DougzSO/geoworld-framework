# 09 — Technical Decisions (moved)

This file's content (D1–D7: config separation, Pydantic schema migration, MCDA extraction, TOPSIS/OWA precedence, centralized styling/reporting, GHG abatement scope, absence of a test suite) has moved to **`docs/BACKLOG.md`**, under the **"Appendix: Technical Decisions"** section at the end of that file.

`BACKLOG.md` (renamed from `refactoring-roadmap.md` during the `docs/` reorganization) became the canonical location for both the BLOCKER task/status tracker (BLOCKER-001 through BLOCKER-012) and this decisions log during a consolidation pass, so blocker fixes that reference or harden a specific decision (e.g. BLOCKER-006 hardening D4) can live next to each other instead of across two separately-maintained files.

No content was rewritten in the move — D1–D7 were relocated verbatim.
